"use strict";
{
    // TypeScript Data Types
    // 1. primitiveType-------
    // number
    let roll = 1;
    // string 
    let className = "Jahid";
    // boolean 
    let isAdmin = false;
    //undefined
    let data = undefined;
    // null 
    let anotherData = null;
    //--- any 
    let AllData = "don't use any type";
    // 2. Non Primitive DataTypes ---------
    // Array 
    let numbers = [2, 3, 4, 5,];
    let students = ['jahid', 'morol', 'zarif'];
    // tuple 
    let ageName = [25, "jahid"];
}
